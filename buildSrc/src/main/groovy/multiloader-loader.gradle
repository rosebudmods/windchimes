plugins {
    id 'multiloader-common'
}

def commonProject = project(":common:${project.name}")

tasks.named('compileJava', JavaCompile) {
    source(commonProject.sourceSets.main.java.sourceDirectories)
}

processResources {
    from(commonProject.sourceSets.main.resources.sourceDirectories)
}